name: Cleanup Old Artifacts

on:
  workflow_dispatch:  # manually trigger করবে

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - uses: geekyeggo/delete-artifact@v6
        with:
          name: |
            app-debug
            app-release
          failOnError: false
